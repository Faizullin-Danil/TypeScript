import { useEffect, useState, useRef } from 'react';
import "./Education.css"

const Education = () => {
    

    return (
        <div>
            <a href="#" className='link'>ссылка</a>
            <input type="text" className='link' placeholder='напиши' disabled/>
        </div>
    );
}

export default Education;
